--
-- Lua configuration script
-- read this script on program startup to specify initial
-- window location and dimensions
--

--The changes below allow lua to parse the settings as a table
--instead of building a text parser and trying to pass it to main.

xpos = 100
ypos = 200
width = 200
height = 200
fullscreen = false


